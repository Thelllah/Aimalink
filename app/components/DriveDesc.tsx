import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'
import Header from './Header'

function DriveDesc() {
  return (
    <SafeAreaView>
        <Header/>
    </SafeAreaView>
  )
}

export default DriveDesc